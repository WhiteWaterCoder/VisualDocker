<UserControl x:Class="VisualDocker.Controls.Common.CommonFilters.CommonFiltersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Show all:" 
                       Grid.Column="0" Grid.Row="0"
                       />
        <CheckBox IsChecked="{Binding ShowAll}"
                      Grid.Column="1" Grid.Row="0"
                      />

        <TextBlock Text="Do not truncate results:" 
                       Grid.Column="3" Grid.Row="0"
                       />
        <CheckBox IsChecked="{Binding DoNotTruncateResults}"
                      Grid.Column="4" Grid.Row="0"
                      />

    </Grid>
    
</UserControl>
